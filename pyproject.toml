[project]
name = "reach-ops"
version = "0.1.0"
description = "Reach-Ops disaster-response accessibility pipeline"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.4.0",
]

[tool.uv]
package = false
