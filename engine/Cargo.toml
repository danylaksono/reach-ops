[package]
name = "reach-ops-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ordered-float = "4"
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
