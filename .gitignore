# Raw and derived geodata (large, re-fetchable — not versioned)
local-data/

# Side-by-side road builds from `pipeline.roads_overture --out ...`, used to
# diff one source against another. data/ is otherwise committed, but a second
# copy of the network is tens of MB and is not a build input.
data/*/roads_*.geojson

# Python
__pycache__/
*.pyc
.venv/
venv/

# Rust
target/

# wasm-bindgen build output (generated from engine/ — run the build step
# in README.md to regenerate)
web/pkg/

# Node
node_modules/
dist/
*.tsbuildinfo

# Generated at build time by web/scripts/copy-data.mjs from the repo-root
# data/ (the committed source of truth) — not itself checked in.
web/public/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Playwright MCP test-run artifacts
.playwright-mcp/
